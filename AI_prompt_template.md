# AI 코드 생성 프롬프트 템플릿

ChatGPT, Gemini, Claude 등에서 고품질 머신러닝 코드를 생성하기 위한 프롬프트입니다.

---

## 🎯 핵심 원칙

1. **구체적인 목표** 제시 (숫자로)
2. **환경 정보** 명시 (Colab, 로컬, 경로 등)
3. **사용 가능/불가능 기법** 명확히 구분
4. **단계별 워크플로우** 지정
5. **코드 스타일** 요청 (주석, 오류 방지)
6. **제약사항** 명시

---

## 📋 복사해서 사용할 프롬프트 (흡연 분류 예시)

```
안녕하세요! 흡연 분류 AI 해커톤에 참여 중입니다. 완전한 머신러닝 파이프라인 코드를 작성해주세요.

===========================================
[1. 프로젝트 정보]
===========================================
- 목표: 신체 특성 데이터로 흡연 여부 분류 (이진 분류)
- 평가 지표: ROC-AUC
- 현재 리더보드 1등 점수: 0.76533
- 목표 점수: 0.766 이상 (1등 초과)
- 데이터 크기: train.csv (7,002명), test.csv (3,001명)
- 특성 16개: 나이, 키(cm), 몸무게(kg), BMI, 시력(좌/우), 청력(좌/우), 충치, 공복혈당, 수축기혈압, 이완기혈압, 중성지방, 혈청크레아티닌, 콜레스테롤, HDL, LDL, 헤모글로빈, 요단백, AST, ALT, 감마GTP
- 타겟(label): 0=비흡연, 1=흡연

===========================================
[2. 작업 환경]
===========================================
- 플랫폼: Google Colab
- Python 버전: 3.x
- 데이터 위치: Google Drive
  - 기본 경로: /content/drive/MyDrive/AI_Projects/smoking_hackathon/
  - train.csv: {기본경로}/data/train.csv
  - test.csv: {기본경로}/data/test.csv
  - sample_submission.csv: {기본경로}/data/sample_submission.csv
  - 결과 저장: {기본경로}/results/

===========================================
[3. 사용 가능한 기법] ✅
===========================================

**데이터 전처리:**
- pandas로 데이터 불러오기
- 기본 정보 확인 (shape, info, describe, head)
- 결측치 처리 (dropna, fillna - 평균/중앙값/최빈값)
- 이상치 처리 (IQR 방법, clip)
- 데이터 분할 (train_test_split, stratify 옵션)
- 스케일링 (StandardScaler, MinMaxScaler)
- 인코딩 (LabelEncoder, OneHotEncoder)

**탐색적 데이터 분석 (EDA):**
- 기술 통계량 확인
- matplotlib, seaborn 시각화
- 히스토그램, 박스플롯, 산점도
- 상관관계 히트맵
- 그룹별 비교 분석

**피처 엔지니어링:**
- 수치형 변환 (제곱, 제곱근, 로그)
- 구간화 (pd.cut, pd.qcut)
- 비율/차이 특성 생성 (예: HDL/LDL 비율)
- 상호작용 특성 (곱셈, 나눗셈)
- 통계 특성 (평균, 표준편차, 최대/최소)
- Feature Selection (SelectKBest, RFE) - 선택사항

**교차 검증:**
- K-Fold Cross Validation
- Stratified K-Fold (불균형 데이터용)
- cross_val_score

**머신러닝 모델:**
- Logistic Regression
- Decision Tree
- Random Forest (배깅)
- XGBoost (부스팅)
- LightGBM (부스팅)
- CatBoost (부스팅)

**앙상블 기법:**
- Voting (Hard/Soft Voting)
- 평균 앙상블 (예측 확률 평균)
- 가중 평균 앙상블
- Stacking (메타 모델)

**하이퍼파라미터 튜닝:**
- GridSearchCV
- RandomizedSearchCV
- Optuna (선택사항)

**모델 평가:**
- ROC-AUC Score
- Accuracy, Precision, Recall, F1-Score
- Confusion Matrix
- 과적합 확인 (Train vs Validation 점수 비교)

**모델 해석:**
- Feature Importance
- SHAP (선택사항)

===========================================
[4. 사용 불가능한 기법] ❌
===========================================
- 딥러닝 (Neural Network, CNN, RNN, Transformer 등)
- AutoML 도구 (Auto-sklearn, H2O, TPOT 등)
- PCA, t-SNE, UMAP 등 차원 축소
- SVM (개념만 알고 실습 안함)
- 시계열 분석 기법
- 텍스트/이미지 처리 기법
- 외부 데이터 결합

===========================================
[5. 코드 작성 규칙] ⚠️
===========================================

**필수 포함사항:**
1. 한글 폰트 설정 코드 (Colab용 나눔폰트)
2. 모든 시각화에 plt.show() 포함
3. 중간 결과를 print()로 확인
4. 각 단계마다 상세한 한글 주석
5. 섹션별로 구분 (# ===== STEP N: 제목 =====)

**오류 방지 코드:**
1. ID 컬럼 제거 시: df.drop(['ID', 'id'], axis=1, errors='ignore')
2. 스케일링: scaler.fit(train) 후 scaler.transform(train/test) 분리
3. 원본 보존: df.copy() 사용
4. 0으로 나누기 방지: (denominator + 1) 또는 (denominator + 0.001)
5. 결측치 처리: fillna(0) 또는 dropna()

**코드 스타일:**
- Jupyter 노트북 형식 (섹션별 분리)
- 각 셀은 독립적으로 실행 가능하게
- 변수명은 영어, 주석은 한글
- 들여쓰기 4칸

===========================================
[6. 요청하는 워크플로우 (12단계)]
===========================================

**STEP 1: 환경 설정**
- 라이브러리 설치 (!pip install xgboost lightgbm catboost)
- Google Drive 마운트
- 경로 설정
- 라이브러리 임포트
- 한글 폰트 설정

**STEP 2: 데이터 로드**
- CSV 파일 읽기
- shape, info, describe, head 확인
- 컬럼 목록 출력
- 결측치 확인
- 타겟 분포 확인

**STEP 3: EDA (탐색적 데이터 분석)**
- 타겟 분포 시각화 (막대, 파이)
- 전체 특성 히스토그램 (4x4 subplot)
- 전체 특성 박스플롯 (이상치 확인)
- 상관관계 히트맵
- 타겟과의 상관관계 출력
- 흡연자 vs 비흡연자 특성 비교 히스토그램
- 그룹별 평균 비교 테이블

**STEP 4: 데이터 전처리**
- 원본 데이터 copy()
- ID 분리 및 제거
- 특성(X)과 타겟(y) 분리
- 이상치 처리 함수 정의 (IQR 클리핑)
- train_test_split (test_size=0.2, stratify=y)
- StandardScaler 적용 (fit은 train만!)

**STEP 5: 피처 엔지니어링**
- create_features() 함수 정의
- 비율 특성: HDL/LDL, HDL/Cholesterol, 혈압비율
- 차이 특성: 수축기-이완기 혈압(맥압), 시력차이
- 구간화: 나이그룹, BMI그룹, 혈당그룹 (pd.cut)
- 상호작용: 나이×헤모글로빈, 나이×중성지방
- 제곱/제곱근 특성
- 로그 변환
- 통계 특성 (건강지표 평균, 표준편차)

**STEP 6: 기본 모델 학습**
- 6개 모델 정의 (LR, DT, RF, XGB, LGB, CatBoost)
- 각 모델 학습 및 예측
- Train/Validation ROC-AUC 계산
- 결과 테이블 출력
- 막대그래프 비교 (목표선 포함)

**STEP 7: 교차 검증**
- StratifiedKFold (n_splits=5)
- 각 모델 cross_val_score
- Mean AUC (+/- std) 출력
- 최고 성능 모델 확인

**STEP 8: 하이퍼파라미터 튜닝**
- XGBoost RandomizedSearchCV (n_iter=50)
- LightGBM RandomizedSearchCV (n_iter=50)
- CatBoost RandomizedSearchCV (n_iter=30)
- Random Forest RandomizedSearchCV (n_iter=50)
- 각 모델 최적 파라미터 및 점수 출력

**STEP 9: 앙상블**
- Soft Voting 앙상블
- 가중 평균 앙상블 (최적 가중치 탐색)
- Stacking 앙상블 (메타모델: LogisticRegression)
- 3가지 앙상블 결과 비교

**STEP 10: 최종 예측**
- 전체 Train 데이터로 최종 스케일링
- 최적화된 모델들 재학습
- Test 데이터 예측
- 가중 평균 앙상블 적용
- submission.csv 생성 및 저장
- 제출 파일 검증 (행 수, 값 범위, 결측치)

**STEP 11: Feature Importance**
- XGBoost, LightGBM Feature Importance 추출
- Top 20 가로 막대그래프
- Top 10 특성 테이블 출력

**STEP 12: 점수 향상 전략**
- 추가 시도 가능한 피처 엔지니어링 제안
- 모델 튜닝 방향 제안
- 앙상블 개선 방안 제안

===========================================
[7. 제출 파일 요구사항]
===========================================
- 파일명: submission_ensemble.csv
- 행 개수: 3,001개
- 컬럼: ID, label
- label 값: 0~1 사이 확률값
- 결측치 없음

===========================================
[8. 최종 요청]
===========================================

위의 모든 조건을 만족하는 **완전한 코드**를 작성해주세요.

1. Jupyter 노트북 형식으로 섹션별로 나눠주세요
2. 각 셀은 독립적으로 실행 가능하게 해주세요
3. 복사-붙여넣기로 바로 실행 가능하게 해주세요
4. 모든 코드에 상세한 한글 주석을 달아주세요
5. 0.76533을 초과하는 점수를 얻을 수 있는 전략을 포함해주세요

코드 전체를 한 번에 출력해주세요. 중간에 "계속하시겠습니까?" 같은 질문 없이 끝까지 작성해주세요.
```

---

## 📋 범용 템플릿 (다른 프로젝트용)

```
안녕하세요! [대회/프로젝트명]에 참여 중입니다. 완전한 머신러닝 파이프라인 코드를 작성해주세요.

===========================================
[1. 프로젝트 정보]
===========================================
- 목표: [무엇을 예측하는지]
- 문제 유형: [이진분류 / 다중분류 / 회귀]
- 평가 지표: [ROC-AUC / Accuracy / RMSE / MAE 등]
- 목표 점수: [구체적인 숫자]
- 데이터:
  - train.csv: [행 수]개 샘플
  - test.csv: [행 수]개 샘플
- 특성: [특성 개수]개
  - [특성1, 특성2, 특성3, ...]
- 타겟: [타겟 컬럼명] - [값의 의미 설명]

===========================================
[2. 작업 환경]
===========================================
- 플랫폼: [Google Colab / Jupyter Notebook / VS Code]
- Python 버전: 3.x
- 데이터 위치: [로컬 경로 또는 Google Drive 경로]
  - train.csv: [경로]
  - test.csv: [경로]
  - sample_submission.csv: [경로]
  - 결과 저장: [경로]

===========================================
[3. 사용 가능한 기법] ✅
===========================================
[사용할 수 있는 라이브러리, 모델, 기법 나열]

예시:
- 전처리: pandas, numpy, sklearn.preprocessing
- 시각화: matplotlib, seaborn
- 모델: RandomForest, XGBoost, LightGBM, CatBoost
- 검증: StratifiedKFold, cross_val_score
- 튜닝: GridSearchCV, RandomizedSearchCV
- 앙상블: VotingClassifier, StackingClassifier

===========================================
[4. 사용 불가능한 기법] ❌
===========================================
[사용하면 안 되는 기법 나열]

예시:
- 딥러닝 (TensorFlow, PyTorch)
- AutoML
- 외부 데이터

===========================================
[5. 코드 작성 규칙] ⚠️
===========================================

**필수:**
- 한글 주석 포함
- 시각화에 plt.show() 포함
- 중간 결과 print() 출력
- 섹션별 구분

**오류 방지:**
- 컬럼 제거 시 errors='ignore'
- 스케일링 fit/transform 분리
- copy() 사용

===========================================
[6. 워크플로우]
===========================================
[원하는 단계별 작업 나열]

1. 환경 설정 및 데이터 로드
2. EDA
3. 전처리
4. 피처 엔지니어링
5. 모델 학습
6. 교차 검증
7. 하이퍼파라미터 튜닝
8. 앙상블
9. 최종 예측 및 제출 파일 생성
10. 점수 향상 전략

===========================================
[7. 최종 요청]
===========================================
- 완전한 코드를 한 번에 출력해주세요
- 중간에 끊지 말고 끝까지 작성해주세요
- 복사-붙여넣기로 바로 실행 가능하게 해주세요
```

---

## 🔧 상황별 추가 프롬프트

### 코드가 중간에 끊겼을 때:
```
코드가 중간에 끊겼습니다. [마지막으로 출력된 부분]부터 이어서 끝까지 작성해주세요.
```

### 오류가 발생했을 때:
```
다음 오류가 발생했습니다:
[오류 메시지 붙여넣기]

이 오류를 해결한 수정된 코드를 작성해주세요.
```

### 성능을 더 높이고 싶을 때:
```
현재 점수: [현재 점수]
목표 점수: [목표 점수]

현재 사용 중인 방법:
[현재 코드 또는 방법 설명]

점수를 높이기 위한 추가 방법을 제안하고, 해당 코드를 작성해주세요.
사용 가능한 기법: [사용 가능한 기법 나열]
```

### 특정 부분만 개선하고 싶을 때:
```
현재 [피처 엔지니어링 / 모델 튜닝 / 앙상블] 부분을 개선하고 싶습니다.

현재 코드:
[현재 코드 붙여넣기]

더 좋은 방법으로 개선해주세요.
```

---

## 💡 프롬프트 작성 팁

### 1. 구체적인 숫자 제시
❌ "좋은 점수를 얻고 싶어요"
✅ "ROC-AUC 0.766 이상을 달성하고 싶습니다"

### 2. 환경 정보 명시
❌ "코드 작성해줘"
✅ "Google Colab에서 실행할 코드를 작성해주세요. 데이터는 Google Drive의 /content/drive/MyDrive/project/ 경로에 있습니다"

### 3. 제약사항 명확히
❌ "머신러닝으로 해결해줘"
✅ "딥러닝은 사용하지 말고, RandomForest, XGBoost, LightGBM만 사용해주세요"

### 4. 출력 형식 지정
❌ "코드 만들어줘"
✅ "Jupyter 노트북 형식으로, 섹션별로 나눠서, 한글 주석과 함께 작성해주세요"

### 5. 완전성 요청
❌ "대충 만들어줘"
✅ "복사-붙여넣기로 바로 실행 가능한 완전한 코드를 작성해주세요. 중간에 끊지 말고 끝까지 작성해주세요"

---

## 🎯 ChatGPT vs Gemini 차이점

### ChatGPT (GPT-4)
- 장점: 코드 품질 높음, 주석 상세
- 단점: 긴 코드 중간에 끊김 가능
- 팁: "Continue" 또는 "이어서 작성해줘"로 계속 요청

### Gemini
- 장점: 긴 코드도 한 번에 출력 가능
- 단점: 가끔 불필요한 설명 추가
- 팁: "설명 없이 코드만 출력해줘" 추가

### Claude
- 장점: 구조화된 코드, 오류 방지 코드 잘 작성
- 단점: 보수적인 답변
- 팁: 구체적인 수치와 조건 제시
